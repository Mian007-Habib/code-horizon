const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;

//PostCSS is a tool that processes your CSS and can apply plugins to do things like add prefixes, minify, etc.
// PostCSS is a tool for transforming CSS with JavaScript plugins.
// It is a JavaScript-based CSS preprocessor that allows you to use plugins to lint your CSS, support variables and mixins, transpile future CSS syntax, inline images, and more